# Contributing

感谢贡献。这个项目以 Agent 应用开发学习和可复现 Demo 为目标，提交应优先改善启动体验、证据可追溯性和测试覆盖，而不是把确定性规则改成不可控的全自主决策。

## 开发环境

- Python 3.12（兼容声明为 Python 3.10+）
- Node.js 18+
- Docker Desktop（仅运行完整容器或 OCR 镜像时需要）
- 默认使用合成 Demo 数据；真实 ERP 集成属于自托管扩展

## 提交前检查

```bash
python -m ruff check .
python -m pytest
cd frontend && npm run build
```

如果改动了规则、Prompt、API Schema、工作流状态或数据格式，请在 PR 描述中写明回归命令和结果。

## 数据和安全要求

- 不提交 `.env`、真实 API Key、银行流水、发票影像、员工/供应商/客户信息或内部制度原文。
- 新增样例必须放在合成 Demo 数据目录，并说明生成方式。
- Agent 工具默认只读；任何改变单据状态的动作必须经过人工终审边界。
- 不在日志、异常响应和截图中输出凭据或完整敏感字段。

## Pull Request 内容

请说明：

1. 修改目的和影响范围；
2. 是否修改 API 契约、Prompt、规则或数据格式；
3. 测试命令和结果；
4. 是否引入外部服务、模型或第三方许可证；
5. 是否检查敏感信息和公开授权边界。
