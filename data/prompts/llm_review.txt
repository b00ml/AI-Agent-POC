# 版本：v2（2026-08-02）｜M2 报销单 AI 复核提示词（core/llm_reviewer.review_claim）
# 工作流内容来自 doc/skills/m2-claim-review/SKILL.md，经 {skill_content} 注入；改工作流改 skill 文件，改本文件只动数据段与输出要求。

请作为财务部 AI 复核助手，严格按照下面的 Skill 工作流执行本环节任务：

{skill_content}

# 制度知识库检索条款（依据当前单据自动检索，带文档出处）
{policy_clauses}

# 单据信息
单据号：{claim_no}
员工职级：{job_level}，部门：{department}
单据类型：{claim_type}，出差城市：{city}，晚数：{nights}
费用行：
{lines}

# 差旅标准（职级+城市档次）
{standards}

# 出差城市档次映射（核验差旅标准前先查本表；当前单据出差城市已标注）
{city_tiers}

# 审批记录
{approvals}

# OCR 票面要素（本地识别结果，优先于系统录入）
{ocr_fields}

# 规则引擎初审意见（仅供参考，需独立判断）
{rules_opinion}

# 输出要求
严格只输出一个 JSON 对象，不要任何其他文字：
{{
  "result": "APPROVE 或 REJECT 或 FLAG",
  "violations": ["违规代码数组，无违规给空数组"],
  "reasons": ["判定理由，1-3 条，具体到费用行/发票"],
  "confidence": 0.0到1.0之间的数字
}}
违规代码只能是：OVER_STANDARD_HOTEL / OVER_STANDARD_MEAL / OVER_STANDARD_CITY_TRANSPORT / OVER_STANDARD_TRANSPORT_CLASS / INVOICE_TITLE_MISMATCH / INVOICE_TAXNO_MISMATCH / DUPLICATE_INVOICE / MISSING_APPROVAL_OVERTIME_TAXI / MISSING_ATTACHMENT / AMOUNT_MISMATCH / ACCOUNT_MISMATCH
